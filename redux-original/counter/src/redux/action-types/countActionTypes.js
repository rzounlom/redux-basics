// Define action types as constants. These are used to identify the actions.
export const INCREMENT = "INCREMENT"; // Action type for incrementing the counter
export const DECREMENT = "DECREMENT"; // Action type for decrementing the counter
